package com.example.myapplication.networkcall

import android.os.Build.VERSION_CODES.BASE
import com.example.myapplication.BuildConfig

// base url helper we can create a constance
object UrlHelper {
    const val BASE_URL = BuildConfig.BASE
}